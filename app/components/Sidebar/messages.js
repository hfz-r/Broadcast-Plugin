import { defineMessages } from 'react-intl';

export const scope = 'broadcast.components.Sidebar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Announcements',
  },
});
